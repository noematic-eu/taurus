{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Fenêtre d’accueil seulement. Les packs (pack-*) n’ont aucune API.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
